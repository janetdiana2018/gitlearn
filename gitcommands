// Initialize git
git init 

// To add file for tracking
git add 'filename'

// To add all files for tracking
git add .

// To check track status of files
git status

// To commit changes in git
git commit -m 'your message'

// To view commit made
git log

// To add remote repo address
git remote add origin 'URI'

// To push changes to remote repo master branch
git push -u origin master